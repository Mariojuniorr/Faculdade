/*Escreva uma função que recebe uma string S e dois valores inteiros não negativos ie j. A função deve atribuir a string S apenas os caracteres contidos no segmento que vai de i a j da string S.*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char S[50], i, j;
    scanf("%s", &S);
    printf("Digite dois valores inteiros não negativos: ");
    if(i>=0 && j>=0){
        
    }
}